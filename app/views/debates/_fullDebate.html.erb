<table class="table">
   <tr>
        <td><button class="btn btn-large btn-info" type="button" data-toggle="modal" data-target="#answerModal">Propose an Answer</button></td>
   </tr>
   <%= render @debate.answers.sort { |a, b| b.voices <=> a.voices } %>
</table>
