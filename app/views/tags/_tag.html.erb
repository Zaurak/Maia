<tr>
        <td> <b>Tag</b></td>
   </tr>
   <tr>
        <td> 
          <div class="row">
            <div class="span8">
              <%= tag.name %>
            </div>
            <div class="span4 pull-right">
              <%= render 'user', contribution: tag %>
            </div>
          </div>
        </td>
   </tr>
   <tr>
        <td>

<% unless current_user?(tag.user) %>
<div class="row">
<div class="span6 offset5 pull-right">
<button class="btn btn-large btn-primary pull-right open-vote" 
        type="button" 
        data-toggle="modal" 
        data-target="#voteModal"
        data-id=<%= tag.id.to_s %>
        data-type=<%= tag.class.name.split('::').last %>>
        Vote
</button>
</div>
</div>
<% end %>

<%= render 'debates/contribute', contribution: tag %>
</td>
</tr>
<%= render tag.objections %>

<%= render tag.justifications %>