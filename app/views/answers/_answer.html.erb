<tr>
        <td> <b>Answer</b></td>
   </tr>
   <tr>
        <td> 
          <div class="row">
            <div class="span10">
              <%= markdown answer.content %>
            </div>
            <div class="span3 pull-right">
              <%= render 'user', contribution: answer %>
            </div>
          </div>
        </td>
   </tr>
   <tr>
        <td>
<div class="row">
<div class="span6 pull-right">
<button class="btn btn-large btn-primary pull-right" type="button" data-toggle="modal" data-target="#voteModal">Vote</button>
</div>
</div>
<%= render 'debates/contribute', contribution: answer %>

<%= render answer.objections %>

<%= render answer.justifications %>
